import styled from 'styled-components/native';
import {FlatList} from 'react-native';

export const Container = styled.SafeAreaView`
  margin: 30px 20px;
  flex: 1;
`;

export const NewsFlatList = styled(FlatList)``;
